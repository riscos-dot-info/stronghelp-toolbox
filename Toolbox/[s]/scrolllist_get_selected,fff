scrolllist_get_selected
#include Style
#Background wimp 1
#Parent scrolllist
Header:	<ScrollList.H=>scrolllist>
#Line
#Wrap off
Syntax:	
#indent 6
{FCode}extern <_kernel_oserror> *scrolllist_get_selected
(
  unsigned int flags,
  <ObjectId> object_id, 
  <ComponentId> component_id,
  int offset, 
  int *new_offset
);

{F}
#indent
#Wrap
Purpose:	Function will return in new_offset the currently selected item in the ScrollList.  Set offset to -1 to search from the beginning of the ScrollList.