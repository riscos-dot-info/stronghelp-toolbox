PrintDboxTemplate data type
#include Style
#Background wimp 1
Defined in:	<printdbox.h=>print_class>
#Line
#Wrap off
Syntax:	
#indent 6
{fcode}typedef struct
\{
  int         flags;
  int         from;
  int         to;
  int         copies;
  int         scale;
  char        *further_options;
  char        *window;
  int         last_page;
} PrintDboxTemplate;
{f}
#indent
#Wrap
Note that the {/}last_page{/} field was introduced in PrintDbox 0.26 and it is optional (the module checks the body size stored in the <ObjectTemplateHeader>).